import {StyleSheet} from 'react-native';

export const globalTheme = StyleSheet.create({
  globalMargin: {
    marginHorizontal: 20,
  },
  loader: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
